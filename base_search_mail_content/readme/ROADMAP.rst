This module limits the *message_content* search functionality strictly
to internal users, taking into consideration constraints in specific model
views like `project.task`. Consequently, portal users no longer have the ability
to search within mail content.
